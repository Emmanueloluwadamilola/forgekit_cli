name: forge_app_modular
description: Generates a Flutter Modular v7 application with feature modules.
version: 0.1.0+1

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  name:
    type: string
    description: The project name.
    prompt: What is the project name?
  org:
    type: string
    default: com.forgecyberlabs
    prompt: What is the organization identifier?
  useRouter:
    type: boolean
    default: false
    prompt: Use named routes?
  useProvider:
    type: boolean
    default: true
    prompt: Use Provider state management?
  useRiverpod:
    type: boolean
    default: false
    prompt: Use Riverpod state management?
  useBloc:
    type: boolean
    default: false
    prompt: Use Bloc state management?
  useCubit:
    type: boolean
    default: false
    prompt: Use Cubit state management?
